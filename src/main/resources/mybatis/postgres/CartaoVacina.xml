<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="br.jrfy.wsminhasvacinas.mappers.CartaoVacinaMapper">
	  <select id="findByNome" resultType="br.jrfy.wsminhasvacinas.models.CartaoVacina">
		     SELECT 
				ci_cartaovacina as id,
			    nm_pet as nomePet,
			    ds_especie as especie,
			    ds_raca as raca,
			    dt_nascimento as nascimento,
			    sexo,
			    ds_cor as cor,
			    dt_criacao as criacao   
			FROM  minhasvacinas.tb_cartaovacina 
			where true
			<if test="nome != null">
				and nm_pet ilike '%'||#{nome}||'%'
		    </if>
		     order by nomePet;
	  </select>
</mapper>